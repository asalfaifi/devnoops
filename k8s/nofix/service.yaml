apiVersion: v1
kind: Service
metadata:
  name: nofix
  namespace: devnoops
  labels:
    app: nofix
    platform: devnoops
spec:
  type: ClusterIP
  selector:
    app: nofix
  ports:
    - port: 9104
      targetPort: 9104
      protocol: TCP
      name: http
