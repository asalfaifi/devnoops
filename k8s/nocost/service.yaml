apiVersion: v1
kind: Service
metadata:
  name: nocost
  namespace: devnoops
  labels:
    app: nocost
    platform: devnoops
spec:
  type: ClusterIP
  selector:
    app: nocost
  ports:
    - port: 9094
      targetPort: 9094
      protocol: TCP
      name: http
