apiVersion: v1
kind: Service
metadata:
  name: noship
  namespace: devnoops
  labels:
    app: noship
    platform: devnoops
spec:
  type: ClusterIP
  selector:
    app: noship
  ports:
    - port: 9093
      targetPort: 9093
      protocol: TCP
      name: http
