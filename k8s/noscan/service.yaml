apiVersion: v1
kind: Service
metadata:
  name: noscan
  namespace: devnoops
  labels:
    app: noscan
    platform: devnoops
spec:
  type: ClusterIP
  selector:
    app: noscan
  ports:
    - port: 9091
      targetPort: 9091
      protocol: TCP
      name: http
