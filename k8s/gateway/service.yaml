apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: devnoops
  labels:
    app: gateway
    platform: devnoops
spec:
  type: NodePort
  selector:
    app: gateway
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30088
      protocol: TCP
      name: http
